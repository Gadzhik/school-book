{
  "name": "@reader/server",
  "version": "0.0.0",
  "private": true,
  "description": "Библиотечный сервер (Фаза 5): Rust axum — REST + OPDS + раздача книг с Range, SQLite. Запуск через cargo",
  "scripts": {
    "dev": "cargo run",
    "build": "cargo build --release",
    "check": "cargo check"
  }
}
